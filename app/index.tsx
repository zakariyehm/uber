import SplashScreen from './splash';

export default function Index() {
  // This is the initial route - just show splash screen
  // Splash screen will handle navigation to home automatically
  return <SplashScreen />;
}

